# Anthropic API key — required by both agents
ANTHROPIC_API_KEY=sk-ant-...

# GitHub personal access token — needed to push branches and trigger downstream workflows
# Scopes required: repo (full), workflow
GH_TOKEN=ghp_...

# Optional: override the path to the gh CLI binary
# GH_BIN=/opt/homebrew/bin/gh
